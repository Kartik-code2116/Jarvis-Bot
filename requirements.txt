# JARVIS v3.0 — requirements
# Install with: pip install -r requirements.txt

# Core (required)
pyautogui>=0.9.54
Pillow>=10.0.0
requests>=2.31.0
python-dotenv>=1.0.0

# Optional but recommended
pygetwindow>=0.0.9     # window management
psutil>=5.9.0          # RAM/CPU/battery stats
pyttsx3>=2.90          # text-to-speech fallback

# Build to .exe (only if packaging)
# pyinstaller>=6.0.0
